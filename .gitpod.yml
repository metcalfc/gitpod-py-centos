ports:
  - port: 5000
    onOpen: open-preview

image:
  file: .gitpod.Dockerfile

tasks:
  - before: |
      export FLASK_APP=flaskr
      export FLASK_ENV=development
    init: |
      pip3 install -e .
      flask init-db
    command: |
      flask run

vscode:
  extensions:
    - ms-python.python
